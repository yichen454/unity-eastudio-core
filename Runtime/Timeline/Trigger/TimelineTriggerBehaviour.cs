using UnityEngine.Playables;

namespace EA.Timeline
{
    /// <summary>per-clip PlayableBehaviour（事件均在绑定的 <see cref="TimelineTrigger"/> 中配置）。</summary>
    public class TimelineTriggerBehaviour : PlayableBehaviour { }
}
